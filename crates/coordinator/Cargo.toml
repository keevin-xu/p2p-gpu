[package]
name = "p2pgpu-coordinator"
edition.workspace = true
version.workspace = true
license.workspace = true

[[bin]]
name = "coordinator"
path = "src/main.rs"

# Phase 1: cargo add tokio --features full
#          cargo add axum --features ws
#          cargo add tracing tracing-subscriber clap anyhow
# Phase 2: cargo add rusqlite --features bundled
[dependencies]
p2pgpu-protocol = { path = "../protocol" }
