[package]
name = "p2pgpu-protocol"
edition.workspace = true
version.workspace = true
license.workspace = true

# Phase 1: cargo add serde --features derive
#          cargo add serde_json uuid --features uuid/v4,uuid/serde
#          cargo add blake3 thiserror
#          cargo add ts-rs --features ts-rs/uuid-impl
[dependencies]
