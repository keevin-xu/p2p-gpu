[package]
name = "p2pgpu-mock-worker"
edition.workspace = true
version.workspace = true
license.workspace = true

[[bin]]
name = "mock-worker"
path = "src/main.rs"

# Phase 2: cargo add tokio --features full
#          cargo add tokio-tungstenite clap anyhow tracing tracing-subscriber
#          cargo add rand rand_chacha   # seeded, reproducible chaos
[dependencies]
p2pgpu-protocol = { path = "../protocol" }
